# VEX AI ELITE

Paper trading platform with optional guarded live mode.

## Quickstart

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Frontend:
```bash
cd frontend
npm install
npm run dev
```

Docker compose from infra directory for combined stack.
